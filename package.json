{
  "name": "hashi-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:frontend": "npm run --prefix packages/frontend dev",
    "dev:backend": "cd packages/backend && uvicorn main:app --reload",
    "build:frontend": "npm run --prefix packages/frontend build",
    "lint": "npm run --prefix packages/frontend lint"
  }
}
